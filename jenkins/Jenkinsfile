pipeline {
    agent {
        docker {
            sh 'docker pull maven'
        }
    }
   stages {
        stage('Build') {
            steps {
                sh 'mvn -X -DskipTests clean package'
            }
        }
    }
}
