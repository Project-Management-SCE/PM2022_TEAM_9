pipeline {
    agent {
        docker {
            image '17-jdk-alpine'
            args '-v /root/.m2:/root/.m2'
        }
    }
   stages {
        stage('Build') {
            steps {
                sh 'mvn -X -DskipTests clean package'
            }
        }
    }
}
